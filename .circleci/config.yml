version: 2.1
orbs:
  aws-cli: circleci/aws-cli@4.0.0
  node: circleci/node@5
executors:
  aws-executor:
    docker:
      - image: 'cimg/node:22.5.1'
    environment:
      AWS_REGION: AWS_REGION_NAME
jobs:
  build:
    executor: aws-cli/default
    steps:
      - aws-cli/setup:
          profile_name: neeraj_agrawal
          role_arn: arn:aws:iam::590376488337:user/neeraj_agrawal
          role_session_name: neeraj_agrawal
  deploy:
    executor: node/default
    steps:
      - checkout
      - run:
          name: 'Environvement variables'
          command: echo ${AWS_REGION_NAME}
      - run:
          name: 'Build CDK App'
          command: | 
            npm install
            npm run build
      - run:
          name: 'cdk_deploy'
          command: account=590376488337 region=us-east-1 npm run ci_deploy
workflow:
  cdk-deployment:
    jobs:
      - build
      - deploy:
          requires:
            - build